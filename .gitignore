# vscode
.vscode 

# Intellij
*.iml
.idea

# npm
node_modules

# Don't include the compiled main.js file in the repo.
# They should be uploaded to GitHub releases instead.
main.js

# Exclude sourcemaps
*.map

# obsidian
data.json

# superpowers scratch workspaces (any level)
.superpowers/

# gstack session state — holds a local auth token and browse logs,
# so this one has to stay ignored in every fresh clone, not just locally.
.gstack/
